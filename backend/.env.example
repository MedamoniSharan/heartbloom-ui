PORT=4000
MONGODB_URI=mongodb://127.0.0.1:27017/magnetic-bliss
JWT_SECRET=change-me

# Razorpay (optional — if unset, checkout skips online payment and uses the previous flow)
# Never commit real keys. Use the Razorpay Dashboard for live/test keys.
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
