PORT=4000
MONGODB_URI=mongodb://127.0.0.1:27017/magnetic-bliss
JWT_SECRET=change-me

# Razorpay (optional — if unset, checkout skips online payment)
# Defaults can live in tracked backend/payment-gateway.env (see README). Values here override that file.
RAZORPAY_KEY_ID=
RAZORPAY_KEY_SECRET=
